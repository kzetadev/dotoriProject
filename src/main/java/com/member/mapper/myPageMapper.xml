<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="myPage">
	<!-- 내가 쓴 댓글 -->
	<select id="comment" resultType="com.member.vo.Member_InfoVo">
		select comment_no, comment_content, comment_date
		where mem_no=(select mem_no from member_info where mem_id = #{mem_id});
	</select>
</mapper>